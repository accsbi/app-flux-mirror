node_modules/
dist/
*.tar.gz
.DS_Store
*.log

# scripts/ is its own nested git repo (and fully captured in the tarball backup)
scripts/
dist-android/

# ローカル検証エビデンス（deploy 不要・GitHub に載せない）
screenshots_log/
dist-enpc/
dist-subpath-test/

# Playwright出力 / バグ由来dir
test-results/
undefined/

# Windows NTFS ADS ゴミ
*:Zone.Identifier
